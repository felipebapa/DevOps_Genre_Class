apiVersion: v1
kind: Service
metadata:
  name: genre-classifier-service
spec:
  type: NodePort
  selector:
    app: genre-classifier
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30850
